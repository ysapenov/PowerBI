This is the test task for Power BI developer position

1) Draw a symbol using Power BI tools, utilizing a measure.
2) Convert the resulting visual into a table, preserving the integrity of the image, but ensure that each symbol is in a separate cell.
3) Create a temporary table for selecting heart colors (with two values – 'red', 'yellow'), and add a slicer with these values to the sheet.
The table with the visual should change the color of the hearts from red to yellow based on the selection in the slicer.
4) If both colors are selected at the same time, the table should display even-numbered hearts as red and odd-numbered ones as yellow.